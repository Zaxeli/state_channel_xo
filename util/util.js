const web3 = require('web3');



module.exports = {
    web3 : require('web3'),
    setweb3 : function(){

    },
    authenticate : function(signedMsg , address) {
    /**
     * signedMsg is an object that contains two properties
     * Example:
     * signedMsg = {
     *      msg: message (text)
     *      signature: signature (web3.eth.sign(text))
     * }
     * 
     *  
     * */ 
        

    }
}